<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Check Norris Jokes</title>
  <link rel="stylesheet" href="styles/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Mulish:ital,wght@0,200..1000;1,200..1000&display=swap"
    rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

</head>

<body>
  <div class="container">
    <div class="heading">
      <h1>Find out what Chuck Norris can do!</h1>
    </div>
    <div class="form">
      <form action="/" id="form" class="form" method="POST">
        <div class="selection-part">
          <select class="form-select form-select-lg mb-3" aria-label="Large select example" name="category">
            <option value="" selected>random category</option>
            <% if (locals.categories) { %>
              <% for (var i=0; i < locals.categories.length; i++) { %>
                <option value=<%=categories[i] %>><%= categories[i] %>
                </option>
                <% } %>
                  <% } %>
          </select>
        </div>
        <div class="button-part">
          <button type="submit" class="form-submit btn btn-success">Find out</button>
        </div>
      </form>
    </div>
    <div class="card">
      <div class="card-body">
        <% if (locals.content) { %>
          <%= content %>
            <% } %>
      </div>
    </div>
  </div>
</body>

</html>